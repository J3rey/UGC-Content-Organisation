# Copy to .env (and set the same values in Vercel → Project → Settings → Environment Variables)
# to enable the top-right sign-in. Without these, the app runs on localStorage only.
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
